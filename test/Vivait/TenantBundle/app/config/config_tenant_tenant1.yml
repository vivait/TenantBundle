imports:
    - { resource: config.yml }

framework:
  router:
    resource: "%kernel.root_dir%/config/routing_tenant1.yml"

vivait_tenant:
  tenant:
    name: "Tenant 1"
    attributes:
      slogan: "We are the best"